<div class="page-header">
  <div class="btn-group pull-right">
    <%= button_to("Overview", segments_path, icon: :list) %>

    <%= dropdown_button("Actions") do %>
      <%= dropdown_link_to("Delete", @segment, data: { confirm: 'Are you sure?', method: :delete }, icon: 'trash') %>
    <% end %>
	</div>

  <h1>Edit Segment</h1>
</div>

<%= render 'form' %>
