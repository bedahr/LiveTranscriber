<div class="page-header">
  <div class="btn-group pull-right">
    <%= button_to("Overview", reviewed_transcriptions_path, :icon => :list) %>
    <%= button_to("Edit", edit_reviewed_transcription_path(@reviewed_transcription), :icon => :edit) %>
	</div>

  <h1>Showing Reviewed Transcription</h1>
</div>

<%= render 'form' %>
